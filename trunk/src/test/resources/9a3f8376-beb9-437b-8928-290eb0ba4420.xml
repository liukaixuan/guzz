<!-- xxxx -->
<sqlMap dbgroup="default">
	<select orm="user" result-class="java.util.HashMap">
		<paramsMapping>
			<map paramName="vip" propName="vip" />
			<map paramName="isVip" type="boolean" />
		</paramsMapping>
	  select count(*) as m_count, @vip from @@user group by @vip having @vip=:isVip
	</select>
</sqlMap>
